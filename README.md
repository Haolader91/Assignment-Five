1️⃣
(

1. Var is an old version, maybe used before ES6, this is not used now
2. let is coming in ES6, let and value can be changed.
3. const is coming in ES6, value cannot be changed, cannot be re-declared, value has to be set when declared
   )
   var: ভার ওল্ড ভার্শন, ES৬ আসার আগে ব্যবহার করা হয়তো, এইটা এখন বেবহার করা হয় না
   let: let ES৬ এ আসছে, let আর ভ্যালু চেঞ্জ করা করা যায়.
   const: const ES৬ এ এসেছে, ভ্যালু চেঞ্জ করা যায় না, পুনরায় declare করা যায় না, ডিক্লেয়ার করলে ভ্যালু সেট করে দিতে হয়

2️⃣
(
(...) spread operator is used,
to copy array, to copy object, to add new data, this works
)
(...)spread operator ব্যবহার করা হয়,
arry copy করতে object copy করতে, নতুন data অ্যাড করতে, এইটা কাজ করে.

3️⃣
(
map() is executed on the element and returns a new array. This modifies the data.
filter() operates on a condition. And returns a new array
forEach() is executed on a loop. Does not return a new array.
)
map() এলিমেন্ট আর ওপর চালানো হয়, নতুন একটা arry return করে. এইটা ডাটা মোডিফাই করে.
filter() condition অনুযায় কাজ করে। এন্ড নতুন একটা arry return করে
forEach() loop চালানোর কাজ করে। নতুন কোনো array রিটার্ন করে না

4️⃣
function কে শর্ট ফর্ম আর লিখতে arow function বেবহার করা হয়, উদারণ () => {}

5️⃣
Template Literals হলো js আর string লেখার একটা আধুনিক পদ্ধতি, Template Literals আর মাধ্যমে multi -line লেখা যায়
